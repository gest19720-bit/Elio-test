const { origin, sendJson, sendOAuthError } = require('./_shared');
module.exports = (req, res) => { try { const base = origin(req); return sendJson(res, 200, { issuer: base, authorization_endpoint: `${base}/oauth/authorize`, token_endpoint: `${base}/oauth/token`, registration_endpoint: `${base}/oauth/register`, client_id_metadata_document_supported: true, response_types_supported: ['code'], grant_types_supported: ['authorization_code', 'refresh_token'], code_challenge_methods_supported: ['S256'], token_endpoint_auth_methods_supported: ['none'], scopes_supported: ['business.read', 'business.write'], service_documentation: `${base}/app/mcp.html` }); } catch (error) { return sendOAuthError(res, error); } };
